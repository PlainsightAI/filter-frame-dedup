################################################################################################################
# This is an example .env file for configuring the frame deduplication filter.
# Copy this file to .env and update the values according to your setup.
#
# How to use this file:
# 1. Copy this file to .env in the same directory
# 2. Set VIDEO_INPUT to the full path of your input video file
#    Example: VIDEO_INPUT=/home/user/videos/input.mp4
#
# 3. Configure filter parameters:
#    - FILTER_HASH_THRESHOLD: Threshold for hash difference (default: 5)
#    - FILTER_MOTION_THRESHOLD: Threshold for motion detection (default: 1200)
#    - FILTER_MIN_TIME_BETWEEN_FRAMES: Minimum time between saved frames in seconds (default: 1.0)
#    - FILTER_SSIM_THRESHOLD: Threshold for SSIM comparison (default: 0.90)
#    - FILTER_ROI: Region of interest in format (x,y,width,height) or None for full image
#    - FILTER_OUTPUT_FOLDER: Directory to save deduplicated frames
#    - FILTER_DEBUG: Enable debug logging (true/false)
#
# 4. Set JFROG_TOKEN if using JFrog repository
#    Example: JFROG_TOKEN=your_jfrog_token_here
#
# 5. Set LOG_LEVEL for logging verbosity (optional)
#    Example: LOG_LEVEL=DEBUG
#
# Note: Make sure you have the required Python packages installed and Docker configured properly
################################################################################################################

# Video input path
VIDEO_INPUT=/path/to/your/input.mp4

# Filter configuration
FILTER_HASH_THRESHOLD=5
FILTER_MOTION_THRESHOLD=1200
FILTER_MIN_TIME_BETWEEN_FRAMES=1.0
FILTER_SSIM_THRESHOLD=0.90
FILTER_ROI=(0,0,150,300)  # Example ROI, set to None for full image (preferable)
FILTER_OUTPUT_FOLDER=./output
FILTER_DEBUG=true

# Authentication and logging
JFROG_TOKEN=your_jfrog_token_here
LOG_LEVEL=DEBUG

# Docker image version (optional)
VERSION=1.0.0

# Frame Deduplication Filter Configuration

# Input/Output Settings
VIDEO_OUTPUT=output/output.mp4

# Additional Notes:
# 1. FILTER_HASH_THRESHOLD: Lower values mean more sensitive to changes (default: 5)
# 2. FILTER_MOTION_THRESHOLD: Lower values mean more sensitive to motion (default: 1200)
# 3. FILTER_MIN_TIME_BETWEEN_FRAMES: Minimum time in seconds between saved frames (default: 1.0)
# 4. FILTER_SSIM_THRESHOLD: Higher values mean more strict similarity check (default: 0.90)
# 5. FILTER_ROI: Region of interest for processing (x, y, width, height)
# 6. FILTER_DEBUG: Enable debug logging (default: False) 